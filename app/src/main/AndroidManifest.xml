<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sound.ampache"
          android:versionCode="4"
          android:versionName="0.9.3" 
          android:installLocation="auto">
  <uses-sdk android:minSdkVersion="1" />
  <uses-permission android:name="android.permission.INTERNET" />
  <application android:label="@string/app_name"
               android:name="amdroid" 
               android:icon="@drawable/amdroid_icon">
    <!-- Activity Group -->
    <activity android:name=".AmdroidActivityGroup" 
    android:theme="@android:style/Theme.NoTitleBar" >
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <!-- Main activities -->
    <activity android:name=".BrowseActivity" android:label="Music" >
	</activity>
	    <activity android:name=".PlaylistsActivity" android:label="Playlists">
	</activity>
	<!-- DashActivity -->
	<activity android:name=".dashActivity" android:label="@string/app_name">
	</activity>
    <!-- Collection Browser
    <activity android:name="collectionActivity">
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
      </intent-filter>
      <meta-data android:name="android.app.default_searchable" android:value=".songSearch" />
    </activity> -->
    <!-- Preferences -->
    <activity android:name="prefsActivity"
              android:label="@string/prefs_name">
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="andriod.intent.category.PREFERENCE" />
      </intent-filter>
    </activity>
    <!-- Playlist Activity -->
    <activity android:name="playlistActivity"
              android:label="@string/play_name">
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
      </intent-filter>
      <!--meta-data android:name="android.app.default_searchable" android:value=".songSearch" /-->
    </activity>
    <!-- Search Activity -->
    <activity android:name="songSearch" android:label="Search">
      <intent-filter>
        <action android:name="android.intent.action.SEARCH" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <meta-data android:name="android.app.searchable" 
                 android:resource="@xml/searchable" />
    </activity>
    <service android:enabled="true" android:name=".service.PlayerService"/>
  </application>
</manifest> 
